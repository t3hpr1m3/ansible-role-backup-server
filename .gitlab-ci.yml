---
image: jdubz/ci-docker-ansible:1.0.1

services:
  - docker:19.03.1-dind

molecule:
  stage: test
  before_script:
    - apk add --update python python-dev py-pip py3-pip
    - ansible --version
  script:
    - molecule test --all
